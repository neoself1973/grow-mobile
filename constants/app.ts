// 起動時にネットワークの応答を待つ上限（ミリ秒）。**この1箇所だけで定義する。**
//
// なぜ要るか（2026-09-07 段5-0 の実測）: データベースが到達不能なとき、起動時の問い合わせは
// 例外にならず**返ってこない**（TCP の接続待ち）。上限が無いと読み込み表示のまま数分止まり、
// 端末内にある下書きにも到達できなかった。
//
// 値の根拠（2026-09-07 実測・iOS シミュレータ・デモ環境）:
//   profiles の読み出し … 1072 / 1136 / 165 / 109 / 250 / 210 ms（最悪 1136ms＝初回起動）
//   daily_reports + conversations … 49〜100ms ／ loadStats まで合計 155〜263ms
//   ∴ 最悪値の3倍は約 3.4 秒。発注の初期値 6000ms はこれを上回るのでそのまま採る
//   （携帯網の遅い回線を切り捨てないため、余裕のあるほうへ倒す）。
export const STARTUP_TIMEOUT_MS = 6000
